import * as dayjs from 'dayjs';

const convertTimeFormat = (time) => (
    dayjs(time).format('YYYY/MM/DD HH:mm:ss')
)

export function formNcReport(rawData, type='month') {
    
}

export function formItemReport(rawData, type='month') {

}
